name: Build and Deploy

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pages: write

jobs:
  build:
    runs-on: ubuntu-latest

    
    steps:
      - name: Checkout SDK Codebase Repo
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.SDK_Codebase_Repository_Branch }}
          repository: 'HamzaOban/testSDK'
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Node.js version
        uses: actions/setup-node@v4
        with:
          node-version: '18.x'

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'adopt'
          java-version: '17'
  
      - name: Publish SDK Version to GitHub
        run: |
          echo "======================================================================="
          echo " StepName:    Publishing new SDK version to GitHub                     "
          echo "======================================================================="
          git config --global user.email "hamzaoban3@gmail.com"
          git config --global user.name "HamzaOban"
          git remote set-url origin https://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@github.com/HamzaOban/TestRelease.git
	        git checkout main
          git status
          echo "status log"

	        git branch -a
          echo "branch log"

	        git pull
          echo "pull log"

	        git add .
	        git commit -m "Version ${SDK_VERSION} release."

	        echo "Publishing target ${1} to Github completed"
	        git push --set-upstream origin main
